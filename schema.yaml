tables:
  - name: orders
    count: 50
    depends_on: ["users"]
    columns:
      id: "uuid()"
      user_id: "random_ref('users')" # UBAH BARIS INI!
      total: "random_int(10000, 500000)"
  - name: users
    count: 100
    depends_on: []
    columns:
      id: "uuid()"
      name: "person_name()"